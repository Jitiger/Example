# Balloon Fight — 3차 수정본

## 실행

1. ZIP을 새 폴더에 풀고 Unity Hub에서 폴더를 추가합니다.
2. Unity 6000.3.22f1로 엽니다.
3. Play 모드가 아닌 상태에서 **Balloon Fight > Prepare Local Co-op**를 실행합니다.
4. 생성된 Assets/Resources/BalloonPrefabConfig를 확인합니다.
5. Assets/Scenes/SampleScene을 열고 실행합니다.

이 메뉴는 1P·2P·적 2종·스테이지 프리팹과 영구 스프라이트 에셋을 생성하고 연결합니다.
이미 존재하는 프리팹/설정 값은 덮어쓰지 않습니다.
Unity가 없는 제작 환경이므로 ZIP 안에 생성 완료된 프리팹이 들어 있는 것은 아닙니다.
메뉴 실행 전에는 기존 런타임 생성 방식으로 동작합니다.

## 설정별 역할

| 에셋 | 설정 |
| --- | --- |
| BalloonGameConfig | 이동/AI/점수/부활/몸체 충돌 크기 |
| BalloonInputConfig | 이동/날갯짓/재시작 키, HUD 자동 반영 |
| BalloonVisualConfig | 배경/발판/풍선 색상·배치 및 교체할 스프라이트 |
| BalloonUiConfig | HUD 위치/문구/글꼴 크기/색상/기준 해상도 |
| BalloonFeedbackConfig | 풍선 파열 조각 개수/크기/속도/수명/풀 크기 |
| BalloonPrefabConfig | 런타임에 사용할 플레이어/적/스테이지 프리팹 |

## 로컬 2인 조작

- 1P: `A / D`, `Space` 또는 `W`
- 2P: `← / →`, `↑` 또는 `Enter`
- 공통 재시작: `R`

두 플레이어의 목숨과 부활은 따로 처리됩니다. 한 명이 전부 쓰러져도 다른
플레이어가 살아 있으면 계속 진행합니다. 1P, 2P, 적은 구분 없이 서로의
풍선을 터뜨릴 수 있습니다.

프리팹 생성 이후 외형과 발판 배치는 생성된 프리팹에서 편집합니다.
VisualConfig의 스프라이트/색상은 생성 시 사용되며 이미 생성된 프리팹을 자동 변경하지 않습니다.
UI와 키 안내는 실행 중 해당 설정을 사용합니다.
풍선 개수를 바꾸면 프리팹의 BalloonTarget 개수도 맞춰야 합니다.

## 변경

- UI 레이아웃·텍스트·색상 분리, 해상도에 맞춘 표시.
- 배경과 캐릭터 색상, 풍선 위치·충돌 설정을 VisualConfig로 분리.
- 프리팹 생성 메뉴 및 런타임 프리팹 우선 사용.
- 임시 픽셀 이미지를 프리팹에서 참조할 수 있는 Sprite/Texture 에셋으로 저장.
- 풍선 파열 효과 풀링: 초기 준비 후 재사용, 수명 종료/재시작 시 반환.
- 효과 풀이 가득 차면 해당 시각 효과만 생략하며 추가 생성하지 않음.
- 기존 Grammer 삭제 및 => 프로퍼티 규칙 유지.

## 검증과 제한

파일/메타파일, 프로퍼티 형식, 설정 참조, ZIP 무결성을 검사했습니다.
Unity Editor가 없어 Unity 컴파일과 Play Mode는 검증하지 않았습니다.
원본 도트 그림 생성 알고리즘의 픽셀 좌표는 임시 이미지 제작 코드에 남아 있습니다.
따라서 모든 코드 리터럴을 제거했다고 주장하지 않습니다.
오디오, 히트 스톱, 카메라 흔들림은 이번 수정 범위가 아닙니다.

## Unity 확인

- Prepare Local Co-op 실행 후 Console 오류와 다섯 프리팹의 Missing Script 여부.
- 에디터 재실행 후 생성 스프라이트 참조 보존.
- 이동/날갯짓, 풍선 파열 후 이펙트 종료.
- 적 사망→다음 페이즈, 부활, 게임오버→재시작 반복.
- 재시작 후 이전 이펙트·죽은 적이 남지 않는지.
- 키 변경과 HUD 안내 일치.
- 메뉴 재실행 시 직접 편집한 기존 에셋 보존.

최신 main의 사용자 추가 자료를 보존하여 3차 리팩터링을 반영합니다.
